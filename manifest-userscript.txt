# Audioplay — the USERSCRIPT build manifest (v138, un-frozen
# 19 Aug 2026 at the owner's request: the backup that works
# if the website's hosting goes away).
#
#   node build.js manifest-userscript.txt lichess_audioplay.js
#
# writes the installable artifact at the repo root, which IS
# committed - unlike the website's index.html there is no
# deploy step to build it for the person installing it, so
# the file itself is the deliverable. The harness rebuilds it
# and fails if the committed copy has drifted from src/ (the
# w18 shape, guarded the way checks.yml used to guard the
# page).
#
# SHARED with the website, byte-identical: everything between
# the header and the footer that does not say "userscript" in
# its name. USERSCRIPT-ONLY: userscript-header (metadata +
# setup notes + IIFE open), userscript-lichess (token via the
# Userscripts app's storage, game id from the URL),
# userscript-ui (the floating row over lichess.org),
# userscript-boot (watch for a game page). The website-only
# files (lichess, board, touch, ui, boot, index.html) are the
# panels Lichess's own page already is, which is the point of
# a userscript.
#
# The frozen v137 build in frozen-userscript/ keeps its own
# manifest, unmaintained, as the record of the old line.
userscript-header.js
settings.js
log.js
watchdog.js
vocabulary.js
parsing.js
matching.js
dialogue.js
practice.js
speech-out.js
chimes.js
mic.js
userscript-lichess.js
userscript-ui.js
rules.js
clock.js
userscript-boot.js
closure-footer.js
